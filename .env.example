# Private Sites auth is supplied by the dispatcher. Never trust client auth IDs.
# Enable only after Neon migrations, restricted role, memberships and tests pass.
ENABLE_COMMERCIAL_API=false
DATABASE_URL=
# Local operator/CI only. NEVER put owner credentials in hosted web runtime.
DIRECT_DATABASE_URL=
